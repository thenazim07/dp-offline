export type Login = {
    contact: string;
    password: string;
};

export type Register = {
    contact: string;
    password: string;
};

export interface LoginResponse {}

export interface RegisterResponse {}
